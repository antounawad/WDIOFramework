
		// var targetUrl;
		// var test; //--Test=irgendwas
		// process.argv.forEach(function(element) {
        //      if (element.indexOf("--targetUrl") > -1) {
        //          targetUrl = str.substr(12);
        //      }
        //      if (element.indexOf("--Test") > -1) {
		// 		test = str.substr(7);
		// 	}
		//    console.log("Tesst: "+element);
		// }, this);


        
		// var targetUrl;
        // for (var index in process.argv) {
        //     var str = process.argv[index];
        //     if (str.indexOf("--targetUrl") == 0) {
        //         targetUrl = str.substr(12);
        //     }
        // }
		// console.log('TargetUrl: '+targetUrl);



		// var i = TargetUrl.indexOf("=");
		// console.log("index: "+i);
		//TargetUrl = TargetUrl.substr(12);
		//console.log("TargetUrl: "+TargetUrl);

		
        // console.log(this.test.fullTitle());


        	// var targetUrl;
        // for (var index in process.argv) {
        //     var str = process.argv[index];
        //     if (str.indexOf("--targetUrl") == 0) {
        //         targetUrl = str.substr(12);
        //     }
        // }
		// console.log('TargetUrl: '+targetUrl);